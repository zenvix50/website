package com.zenvix.monitoring;

/**
 * Standard enum tracking the explicit lifecycle of active service states
 * routing automated notification hooks securely dynamically.
 */
public enum HealthState {
    HEALTHY,
    UNHEALTHY,
    CRASHED,
    CIRCUIT_OPEN
}
