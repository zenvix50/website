import { n as __exportAll } from "./chunk-CYJPkc-J.js";
//#region node_modules/refractor/lang/diff.js
var diff_exports = /* @__PURE__ */ __exportAll({ default: () => diff });
/**
* @import {Refractor} from '../lib/core.js'
*/
diff.displayName = "diff";
diff.aliases = [];
/** @param {Refractor} Prism */
function diff(Prism) {
	(function(Prism) {
		Prism.languages.diff = { coord: [
			/^(?:\*{3}|-{3}|\+{3}).*$/m,
			/^@@.*@@$/m,
			/^\d.*$/m
		] };
		/**
		* A map from the name of a block to its line prefix.
		*
		* @type {Object<string, string>}
		*/
		var PREFIXES = {
			"deleted-sign": "-",
			"deleted-arrow": "<",
			"inserted-sign": "+",
			"inserted-arrow": ">",
			unchanged: " ",
			diff: "!"
		};
		Object.keys(PREFIXES).forEach(function(name) {
			var prefix = PREFIXES[name];
			var alias = [];
			if (!/^\w+$/.test(name)) alias.push(/\w+/.exec(name)[0]);
			if (name === "diff") alias.push("bold");
			Prism.languages.diff[name] = {
				pattern: RegExp("^(?:[" + prefix + "].*(?:\r\n?|\n|(?![\\s\\S])))+", "m"),
				alias,
				inside: {
					line: {
						pattern: /(.)(?=[\s\S]).*(?:\r\n?|\n)?/,
						lookbehind: true
					},
					prefix: {
						pattern: /[\s\S]/,
						alias: /\w+/.exec(name)[0]
					}
				}
			};
		});
		Object.defineProperty(Prism.languages.diff, "PREFIXES", { value: PREFIXES });
	})(Prism);
}
//#endregion
export { diff_exports as n, diff as t };

//# sourceMappingURL=diff-DKLK8owr.js.map